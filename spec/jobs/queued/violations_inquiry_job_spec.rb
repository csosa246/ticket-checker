require 'rails_helper'

# RSpec.describe Queued::ViolationsInquiryJob do
  # let(:job) { described_class.new }
  # let(:municipalities) { Municipality.all }

  # describe "#perform" do
    # subject { -> { job.perform(package) } }

    # it { should change { Ticket.where(municipality_id: municipality.first.id).count }.by(+1) }

    # # let(:package) { orders(:order_without_delivery).packages.first }

    # # it { should change(package, :pickup_task).to be }
    # # it { should change(package, :dropoff_task).to be }
    # # it { should enqueue_job(Queued::CreateLegacyDeliveryRequestJob).with(package) }
  # end
# end

